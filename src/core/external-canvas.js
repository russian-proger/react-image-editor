const ImageEditorCore = require('./');

/**
 * External canvas
 * @this {ImageEditorCore}
 */
function ExternalCanvas() {
  this.renderExternalCanvas = function() {
    
  }

  // First rendering
  this.renderExternalCanvas();
}

module.exports = ExternalCanvas;